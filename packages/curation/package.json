{
  "name": "@whippin/curation",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "curate": "uv run scripts/curate.py",
    "shelf:lyrics": "uv run scripts/shelf_lyrics.py",
    "test": "uv run --no-project --with pytest pytest -q tests"
  }
}
