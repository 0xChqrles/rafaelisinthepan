// Shared result choreography: the word result rises into the keyboard's vacated tray,
// the sentence's stage rises into the whole column the dissolved sentence handed over —
// same rise, same tally beat. Keep these numbers aligned with `.solved-results` (Word),
// and `.solved-stage` + its card `.solved-numbers` (the sentence), whose tally waits out
// the card's own rise.
export const RESULTS_IN_MS = 250;
export const SCORE_COUNT_MS = 800;
