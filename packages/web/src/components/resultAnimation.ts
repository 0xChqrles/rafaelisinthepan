// Shared result choreography: both modes' results rise into the keyboard's vacated tray —
// same rise, same tally beat. Keep these numbers aligned with `.solved-results` (Word) and
// `.solved-numbers` (the sentence).
export const RESULTS_IN_MS = 250;
export const SCORE_COUNT_MS = 800;
