// Shared result-stack choreography: both daily modes rise into the keyboard's vacated
// tray, then tally their named score. Keep these numbers aligned with `.solved-results`.
export const RESULTS_IN_MS = 250;
export const SCORE_COUNT_MS = 800;
